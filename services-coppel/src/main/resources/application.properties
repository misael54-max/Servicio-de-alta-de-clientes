spring.application.name=services-coppel
server.port=8080
# --- Datasource (H2 in-memory for dev) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/coppel_db
spring.datasource.username=postgres
spring.datasource.password=Misael15
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- H2 console ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT settings (must match auth-services)
jwt.secret=change-this-dev-secret-please
jwt.issuer=auth-services
